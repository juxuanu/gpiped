# Alternative Piped frontend

It aims to be beautiful, fast and mobile-friendly.

## Development

You need to set up your env vars. Start with
```bash
cp env.local.template .env.local
```
and modify accordingly (the defaults use `kevin.rocks`).
